# Changelog

All notable changes to PR Marmot are recorded here. Generated by [git-cliff](https://git-cliff.org) from the commit history — do not edit by hand.


## [0.12.1] — 2026-09-25

### 🚀 Features

- Name the desktop release a core build ships in (`97e215c`)
- Say which commit an XCFramework was built from (`c738b3c`)
- Share the rate-limit reserve pause with the iPad (`a3e1a86`)

### 🐛 Bug Fixes

- Wait as long as GitHub says after a rate limit (`bcbc467`)
- Percent-encode a matched issue ID in its link (`b887d5b`)
- Clamp an absurd clock in snooze and wake_due (`9199bb4`)

### 📚 Documentation

- FEATURES names the v0.12.1 rate-limit wait (`4f8f874`)
- Record the issue-link and rate-limit fixes in HANDOFF (`439da89`)
- The iPad app is free for seven days, then bought once (`141427a`)
- Record the iPad round-5 core fixes in HANDOFF (`a90c32d`)
- V0.12.0 is the shipped build (`b60cb9b`)

## [0.12.0] — 2026-09-22

### 🚀 Features

- Share All open's observe rule with the iPad (`2e928aa`)
- Put the sections in your own order, in config.toml and Settings (`e323371`)
- One name per section in every view (`534b4c0`)
- All open, every open PR in one repository (`c4f97a1`)
- The snooze menu, size band and Details line kinds from core (`ebc99d0`)

### 🐛 Bug Fixes

- The keyboard shortcuts list names All open's key 3 (`fba80b7`)
- Every count is the open view's, and "need you" is the badge's rule (`b23d256`)
- Keep a teammate's PR you reviewed out of Available to review (`9ecad33`)
- Drop a page fetched before reset, and never abort on a Swift error (`e1ee9a6`)

### 📚 Documentation

- Mark the v0.12.0 features released (`356efcf`)
- Record the reset guard, the callback error and the new exports (`1c6ed18`)
- Mark the v0.11.0 features released (`1fce395`)

## [0.11.0] — 2026-09-21

### 🚀 Features

- One sentence for the code screen's wait, owned by core (`2084f65`)
- Say what each kind of token reaches, and offer the gh login (`4fd1c87`)

### 🐛 Bug Fixes

- Name a fine-grained token's resource owner, not "a token you own" (`53053d5`)
- Say "1 unresolved comment", not "1 unresolved comments" (`c87d4f8`)
- Show a snooze deadline in the reader's time zone (`c4fd962`)
- Show only GitHub's JSON message after an HTTP error's status line (`8b9d5c8`)

### 📚 Documentation

- The code is entered, not typed, in the waiting note's comment too (`af7c508`)
- Say the memory gate is measured wherever it still said pending (`3bc279c`)
- G0 passes on a 37.4 h soak of the v0.9.1 build (`59921a6`)
- The iPad app is in development ahead of G1 (`4678e19`)
- Mark the v0.10.0 features released (`b39ed2a`)

## [0.10.0] — 2026-09-18

### 🚀 Features

- Say on the sign-in code screen that organizations may need to approve PR Marmot (`e63c08e`)

### 🐛 Bug Fixes

- Load the direnv shell from git-tracked files only (`76bf3e3`)
- List every ignored config value in the app's banner (`d4cc41d`)

### 📚 Documentation

- The iPad app is bought once, no trial (`6aad2e9`)
- Mark the v0.9.1 features released (`bf419bd`)

### 🧹 Chores

- Add Nix flake and direnv development environment (#19) (`aac581a`)
- Footprint sampler and G0 verdict for the memory soak (`a85fc6c`)

## [0.9.1] — 2026-09-18

### 🚀 Features

- Keep a known merge conflict while GitHub is still deciding (`e9e5422`)
- Unwatch a PR by its id, without its row (`b32e7dc`)
- Say under the header what the token could not read (`e521e5b`)
- One line for what a token was not allowed to read (`e141303`)
- Tell a refused pull-request field apart from a failed query (`5ecf24d`)

### 🐛 Bug Fixes

- Say in the app when config.toml is ignored (`edea02f`)
- Open a PR the board isn't showing when its notification is clicked (`56266ca`)
- No "Waiting on" snooze on your own PRs (`af22059`)
- Use the Enterprise host typed on the sign-in screen (`b97146e`)
- Check for updates without the GitHub CLI (`59f4f47`)
- Use the GitHub CLI login before PR Marmot's own sign-in (`7b50042`)
- A fine-grained token gets its board, with CI shown as hidden (`107aacd`)
- A missing GitHub CLI no longer reads as the only way to sign in (`680a1dd`)
- The GitHub CLI is optional, so the cask and installer stop requiring it (`2e85364`)

### 📚 Documentation

- Make two FEATURES.md claims exact (`aff1f37`)
- List every feature with a stable id in FEATURES.md (`5ff5d7a`)
- Say what a fine-grained token can't read, and what PR Marmot shows then (`ab9f36b`)

### 📦 Dependencies

- Update async-compression, cfg-if, compression-codecs, rustix, serde-saphyr, syn, and unicode-ident (`75b2bcb`)

### 📝 Other

- Keep a known merge conflict while GitHub is still deciding, for the iPad (`9d42ec3`)
- Unwatch a PR by its id, for the iPad's list of watches (`38ea857`)
- A fine-grained token gets its board, with CI shown as hidden (`f4a776b`)

## [0.9.0] — 2026-09-18

### 🚀 Features

- Say how alarming a section is, so both front ends decorate it alike (`ef095b5`)
- Move the board's cells into core, so both front ends say the same (`a2744b8`)
- Ship PR Marmot's own GitHub registration, so signing in needs no setup (`2f37ac2`)
- Share the empty-queue wording, so neither front end invents one (`b0d58ad`)
- Share the header sentence, the toggles and the clock words (`1f95260`)
- Move the Details panel's lines into core so both front ends say the same thing (`29f70b9`)
- Give the iPad the desktop's watch, snooze and config file (`187675c`)
- Let Swift keep an offline board without redefining a PullRequest (`cebcf13`)
- Expose the device flow so the iPad can sign in (`e83c3f5`)
- Give PR Marmot for iPad the desktop's core instead of a copy of it (`45a77db`)
- Sign in to GitHub without the gh CLI, on github.com or Enterprise (`fa9dbc7`)

### 🐛 Bug Fixes

- Match the smoke package's deployment target to the library's (`49dc797`)

### ⚡ Performance

- Repaint the idle window once a minute, not every 5 s (`8f71da6`)

### 🚜 Refactor

- Move the search grammar into core and give the CLI --filter (`711d4ef`)

### 📚 Documentation

- Publish the v0.8.1 memory soak, threshold by threshold (`ff93891`)
- Cloud pushes to the iPad app, the one PR Marmot ships (`cd52dfc`)
- The paid app is PR Marmot for iPad, with no iPhone in v1 (`e7cd9bb`)
- Regenerate the changelog for the unreleased work (`fd58249`)
- Both registrations are first-class, and say which to prefer (`e766255`)
- Record that sign-in is an OAuth App, and why (`3eca920`)
- Record the shared-wording modules and the fixture's missing review (`9a3d6df`)
- Record the v0.8.1 state and the passed memory gate (`919891d`)

### 🧹 Chores

- A scenario with enough rows to fill a screen (`092ceff`)
- Give the viewer's review a commit, and a sixth page to stop at (`d716506`)
- Make the no-clock rule enforceable instead of remembered (`99a1749`)

### 📝 Other

- Core says how alarming a section is, for both front ends (`41af3c3`)
- Move the board's cells into core, so the iPad and the desktop say the same thing (`050180f`)
- PR Marmot for iPad's Rust side, shared wording, and GitHub sign-in (`a58171c`)

## [0.8.1] — 2026-09-16

### 🐛 Bug Fixes

- Count "need you" for the view on screen, and show review states and waits in plain words (`d9a89a3`)

### 📚 Documentation

- Mark the v0.8.0 features released (`2d0ff97`)
- Refresh the README screenshots for v0.8.0 (`be88cdf`)

## [0.8.0] — 2026-09-16

### 🚀 Features

- Show a size band (Small, Medium, Large) for each PR and sort the review queue smallest first (`d535cf6`)
- Show how long each PR has waited for a reviewer, filter stale ones, and name team requests nobody answered (`8bb638a`)
- Filter by label, author, or repo with chips, open search with ⌘F or Edit → Find, and count Changed and Snoozed PRs (`eaf2cac`)
- Add prmarmot-cli watch --pr --until and --timeout, JSON Schemas for its output, and shell completions (`2a0a6ce`)

### 🐛 Bug Fixes

- Name the app PR Marmot in the menu bar, Finder, and the Dock (`01c6c1d`)
- Remove the draft badge that collided with the PR column's icons (`25f5475`)
- Keep an approval or change request when the reviewer comments afterwards (`7772add`)

### 📚 Documentation

- Update changelog for size bands, pickup age, filter chips, and CLI waits (`e3e8303`)
- Mark pickup age, size band, watch --until, schemas, and completions as built in the roadmap (`9a04b77`)
- Update changelog for the public roadmap and adoption scripts (`d3c845c`)
- Make ROADMAP.md the public roadmap (`ee79210`)

### 📦 Dependencies

- Update cocoa, granit-parser, redox_users, synstructure, yoke-derive, zerofrom-derive, and zlib-rs (`05fe523`)

### 🧹 Chores

- Add a perf build feature and a demo scale mode for frame-time and table-scale measurements (`bbf7798`)
- Add GitHub issue templates (`3da57af`)
- Add the Discussions setup and a no-telemetry adoption snapshot (`ba3cbe6`)

## [0.7.1] — 2026-09-16

### 🐛 Bug Fixes

- Install to /Applications everywhere so a Mac has one PR Marmot (`9d2efa9`)

### 📚 Documentation

- Update changelog for the single install location (`946f53e`)

## [0.7.0] — 2026-09-16

### 🚀 Features

- Add prmarmot-cli, a read-only terminal and agent front end (`7abd23b`)
- Per-repo reviewer suggestions, authored-only search, conflict carry-forward (`b571a3e`)
- Put the marmot in Settings and rework the notification prompt (`fce8b24`)

### 🐛 Bug Fixes

- Survive PRs you can no longer see and name a missing repository (`b84bdeb`)
- Use symbol form for cask macOS requirement (`f00ecd5`)

### 🚜 Refactor

- Own the board layout so both front ends agree on it (`f99c668`)

### 📚 Documentation

- Refresh the README screenshots (`b1181a9`)
- Document the CLI, per-repo reviewers, and the signed release flow (`e854890`)
- Record the product ladder, the mascot rule, and the workspace split (`c67e70e`)

### 🧹 Chores

- Ship prmarmot-cli inside the app bundle and link it on install (`db528d5`)

## [0.6.0] — 2026-09-15

### 🚀 Features

- Explain what changed in the blue changed-marker tooltip (`5284657`)
- Copy a PR group for Slack, docs, and GitHub (`4fdf710`)
- Add reusable PR Marmot logos and icons (`202ddce`)
- Prepare PR Marmot attention workflow and signed releases (`2338978`)

### 🐛 Bug Fixes

- Patch block 0.1.6 static rejected by future Rust (`6ab63d4`)

### 📚 Documentation

- Update changelog for changed-marker tooltip and block patch (`4c33cc4`)
- Update changelog for group copy (`7564f6f`)
- Always publish releases as latest (`de08430`)

### 📦 Dependencies

- Move GPUI Kit to 0.6.1 and gpui-pre to 0.3.5, refresh lockfile (`1a9445e`)

## [0.5.3] — 2026-09-10

### 🐛 Bug Fixes

- Allow copying PR numbers and details (`4aa1595`)

### 🧹 Chores

- Preserve stack labels across both PR tabs (`be9f19c`)

## [0.5.2] — 2026-09-10

### 🐛 Bug Fixes

- Prioritize approved PRs within Needs action (`eb01c03`)

## [0.5.1] — 2026-09-09

### 🐛 Bug Fixes

- Surface approved PRs at the top of My PRs (`a142140`)

## [0.5.0] — 2026-09-09

### 🚀 Features

- Adopt a scalable pull-request app icon (`d50a17a`)
- Add editable settings and reliable native app controls (`58f4489`)

### 📚 Documentation

- Explain setup and workflows with fictional app screenshots (`5bf18db`)

## [0.4.0] — 2026-09-08

### 🚀 Features

- Pin frequent repositories for one-click switching (`0b5a1d3`)

## [0.3.0] — 2026-09-08

### 🚀 Features

- Filter and page PR queues with selection-aware details (`cf9478e`)
- Surface accessible repos and stacked review opportunities (`3af9057`)

### 🧹 Chores

- Clarify category and stack hierarchy (`78e2ef0`)

## [0.2.1] — 2026-07-27

### 🐛 Bug Fixes

- Make prebuilt macOS setup launchable (`6310461`)
- Paint a real ellipsis on truncated Title/Review/Note cells (`f4e99af`)

### 🧹 Chores

- Configure Amp orb lifecycle (`99de12a`)
- Add pre-push hook that runs the full-workspace verify gate (`703aa1b`)

## [0.2.0] — 2026-07-24

### 🚀 Features

- Exception-first Note plus responsive, resizable columns (`6be7894`)
- Add structured Blocker twin of the action note (`882c3ef`)
- Instant queue switching, section headers, single-click links (`0ced76a`)

### 🐛 Bug Fixes

- Delete unreleased drafts by id so they can't duplicate (`258eb33`)
- Harden queue state, selection identity, and link events (`72e7024`)

### 📚 Documentation

- Record Step 0.11 (note hierarchy + responsive columns) (`53061ba`)
- Scrub internal tracker and org references (`3f69f6a`)

### 📦 Dependencies

- Bump dirs from 5.0.1 to 6.0.0 (#4) (`cd5e1be`)
- Bump toml from 0.8.23 to 1.1.3+spec-1.1.0 (#3) (`cbd99d1`)
- Bump toml_edit from 0.22.27 to 0.25.13+spec-1.1.0 (#2) (`fa9cac9`)
- Bump actions/checkout from 4 to 7 in the actions group (#1) (`4a3f3e8`)

### 🧹 Chores

- Genericize tracker and reviewer examples in-code (`6baeff7`)
- Make the parity oracle and fixtures tracker-agnostic (`2defb40`)
- Upgrade skill to full end-to-end release flow (`b5d1dab`)
- Declare rustfmt+clippy components in rust-toolchain.toml (`c05a46b`)
- Add lint gate, CI, git hooks, and git-cliff release tooling (`62e9f1b`)

### 📝 Other

- Update CLAUDE.md to current state; add verify-ui and cut-release skills (`7d23d6e`)
- Gitignore .claude/research and .claude/scriptorium (`a34e65a`)
- Record publication at oliver-kriska/prboard + release path (`8f7540e`)
- Use the releases list, not /latest, which hides pre-releases (`a343424`)

## [0.1.0] — 2026-07-24

### 📝 Other

- Add curl-able install.sh and a real README install story (`79a8765`)
- Record Step 0.7/0.8 done, extend post-gate roadmap (`e649088`)
- Normalize formatting with cargo fmt (`37626cb`)
- Replace hidden v-toggle with visible My PRs | Review queue switcher (`e018011`)
- Add gh timeout, sync-label tick, and full state persistence (`64c5a51`)
- Fix viewport-clipped Note column and the dismissed-review glyph (`ea0eada`)
- Queue three discovered gaps in HANDOFF (gh timeout, sync-label staleness, state persistence) (`176e3f2`)
- Add DESIGN.md from impeccable init (`572c53f`)
- Apply the top impeccable-critique findings (`f73275a`)
- Add in-app authored/review view switching (v key) (`8f08e67`)
- Disable the table's default outer border at full bleed (`77820b6`)
- Update CLAUDE.md for config file, design module, and bundling (`ba6dfe4`)
- Add macOS .app bundling and Homebrew release scaffolding (`64f87da`)
- Apply the Guise-informed visual design (`6720d42`)
- Add config file, repo picker, label chips, and review-cell fixes (`4003f2b`)
- Feedback round: note tooltips + width priority, theme modes, small polish (`d00cc9e`)
- Add memory-gate tooling, README, and Step-0 build findings (`99439ec`)
- Add GPUI walking skeleton: window, virtualized board table, refresh loop (`76870ef`)
- Add prboard-core: transport traits, query model, board derivation with golden parity tests (`6584bdb`)
- Add research handoff and evidence docs (`1d092af`)

