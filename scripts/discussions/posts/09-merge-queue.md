# Show merge-queue state instead of "approved, waiting for you to merge"
category: Ideas

Today: a PR sitting in a merge queue looks approved and ready, which invites the one click you must not make.

A real "in merge queue, position N" state in the app and in the CLI JSON, from a single extra GraphQL field.

Vote with 👍 if your repos use merge queues.
