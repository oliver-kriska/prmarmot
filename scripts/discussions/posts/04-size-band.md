# Small / Medium / Large size band for every PR (no AI, no minutes)
category: Ideas

Today: rows carry no size information at all.

A deterministic band computed from additions, deletions and changed files, discounting lockfiles, generated and vendored paths. Sort the review queue smallest first. No AI, never a minute estimate.

Vote with 👍. Comment with the paths you would want discounted by default.
